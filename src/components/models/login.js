class Login {
    constructor(username, password,RoleId,IsDisable,id) {
        this.username = username;
        this.password = password;
        this.RoleId = RoleId;
        this.IsDisable = IsDisable;     
        this.id = id;
    }
}

export default Login;